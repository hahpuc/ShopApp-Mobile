import 'package:flutter/material.dart';

class CompleteOrderAdmin extends StatelessWidget {
  const CompleteOrderAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
